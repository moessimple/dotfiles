# Quality-gate and tooling paths synced from moessimple/laravel-starter-kit.
# One path per line, "#" starts a comment, blank lines ignored. classify.sh
# compares each against origin/<default-branch>. When the kit adds a gate file,
# add its path here; classify.sh flags any kit path at the repo root or under
# .github/ that is not listed as `ungrouped` so the omission is visible, not
# silent.
#
# Manifests (composer.json, package.json) and lockfiles are NOT listed: they go
# through plan-manifest.sh and the script-alias realignment instead.

# Split CI
.github/workflows/lint.yml
.github/workflows/static.yml
.github/workflows/tests.yml
.github/actions/setup-app/action.yml
.github/dependabot.yml

# PHP static analysis and style
phpstan.neon
rector.php
pint.json
.gitattributes

# PHP tests
phpunit.xml

# Frontend tooling
vite.config.ts
vitest.config.ts
vitest.setup.ts
tsconfig.json
.editorconfig
.npmrc
.nvmrc
pnpm-workspace.yaml

# Runtime hardening (config only; the require entry and bootstrap/app.php wiring
# are never-touch follow-ups)
config/essentials.php
